<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ld.gg.dao.champ.Champ_dao">
	<select id="champ_rank" resultType="champ_analytics">
		select * from
		champion_analytics
		where teamPosition = #{lane} and summonerTier =
		#{tier}
		order by win desc;
	</select>

	<select id="champ_match_up" resultType="champ_match_up">
		select * from
		champion_match_up
		where left_champion = #{left_champion}
		order by
		match_up_win_rate desc;
	</select>

	<select id="champ_search" resultType="Integer">
		select champion_id from
		champion_info
		where champion_kr_name = #{champion_kr_name}
	</select>

	<select id="champ_recom" resultType="champ_match_up">
		SELECT * FROM champion_match_up
		WHERE RIGHT_CHAMPION = #{right_champion}
		AND LEFT_CHAMPION IN (
			SELECT champion_id
			FROM champion_info
			WHERE champion_tag LIKE '%${tag}%'
			AND MATCH_UP_LANE = #{lane}
		);
	</select>
</mapper>